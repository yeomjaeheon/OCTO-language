//주석 테스트

def fibo(n):
    def calcul(a, b, t, end, res):
        if t < end:
            return calcul(b, a + b, t + 1, end, res + [b])
        else:
            return res + [b]
    
    return calcul(0, 1, 1, n, [])

def main():
    n = 10
    result = fibo(n)